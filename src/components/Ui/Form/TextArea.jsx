import React from 'react'

const TextArea = () => {
  return (
    <textarea
    id={id}
    className={className}
    placeholder={placeholder}
    style={style}
    onChange={onChange}
    value={value}
    />
  )
}
export default TextArea
